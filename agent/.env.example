# omp 环境变量模板 — 复制为 .env 并填入真实值
# cp ~/.omp/agent/.env.example ~/.omp/agent/.env
# 注意: .env 绝不提交进 git

CCH_API_KEY=sk-your-key-here
# 搜索 provider 免费档 key（留空 = 未配置，不生效）
TAVILY_API_KEY=your-tavily-key
EXA_API_KEY=your-exa-key
